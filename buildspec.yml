version: 0.2
#test
phases:
  install:
    commands:
      - npm install
      - echo "test699080-123456789101"
      - sleep 5s
  build:
    commands:
      - npm test
artifacts:
  files:
    - '**/*'
